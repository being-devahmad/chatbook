// Create a React app with a
// Context APl to manage the theme (light/dark mode) across components


import React from 'react'

const DarkMode = () => {
    return (
        <div>

        </div>
    )
}

export default DarkMode
